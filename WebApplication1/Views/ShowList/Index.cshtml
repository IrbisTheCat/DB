@model IEnumerable<WebApplication1.Models.ShowList>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align:center;">Shows I Attended</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Performance.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
>
        <td>
            @Html.ActionLink(item.Performance.Name, "Index", "PerformanceComments", new { perfId = item.Id }, null)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<button >
    @Html.ActionLink("Create New", "Create")
</button>

