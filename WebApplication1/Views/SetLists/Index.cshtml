@model IEnumerable<WebApplication1.Models.SetList>

@{
    ViewBag.Title = "Index";
}

<h2>Edit Set List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Performance.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Song.SongName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Performance.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Song.SongName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
